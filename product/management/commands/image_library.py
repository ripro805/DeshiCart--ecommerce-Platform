# Per-category Unsplash photo ID pool.
# Each category maps to a list of Unsplash photo IDs. URLs are assembled as:
# https://images.unsplash.com/photo-{id}?w=600&h=600&fit=crop
# All photo IDs are real Unsplash IDs (verified public photos).
CATEGORY_IMAGES = {
